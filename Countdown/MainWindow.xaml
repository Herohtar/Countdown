<Window x:Class="Countdown.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Countdown"
        mc:Ignorable="d"
        Title="Countdown" Height="350" Width="525" AllowsTransparency="True" WindowStyle="None" Background="#00FFFFFF" ShowInTaskbar="False" WindowState="Maximized" Topmost="True">
    <Grid>
        <TextBlock Margin="25" Text="{Binding TimeLeft}" Foreground="#FFFFFFFF" FontWeight="Bold" FontSize="24" VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="4" Color="Black" Opacity="1" />
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
